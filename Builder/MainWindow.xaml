<Window x:Class="Builder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:Builder.View"
        xmlns:local="clr-namespace:Builder"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid FlowDirection="LeftToRight" >
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>
            <view:TestBuilderView x:Name="TestBuilderView" Grid.Row="0"/>
            <UniformGrid Columns="3" Grid.Row="1">
                <Button x:Name="AddRowButton" Click="AddRowButton_Click" Grid.Column="0">New rule</Button>
                <Button x:Name="GenerateXML" Grid.Column="1" Click="GenerateXML_Click">Generate XML</Button>
                <Button x:Name="LoadXML" Grid.Column="2" Click="LoadXML_Click">Load XML</Button>
            </UniformGrid>
        </Grid>
    </Grid>
</Window>
